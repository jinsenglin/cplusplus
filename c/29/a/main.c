#include <stdio.h>
#include <stdbool.h>

int main() {
    int T = 0;
    scanf("%d", &T);

    for (int t=0; t<T; t++) {
        int n = 2;
        scanf("%d", &n);

        float A[n][n];
        int X[n];
        int p;
        for (int i=0; i<n; i++) {
            scanf("%d", &X[i]);
        }
        scanf("%d", &p);
    }
}
