#include <stdio.h>
#include <stdbool.h>

int main() {
    int T;
    scanf("%d", &T);

    for (int i=0; i<T; i++) {
        int n, m;
        scanf("%d %d", &n, &m);

        int matrix[1000][1000] = { 0 };
        for (int j=0; j<m; j++) {
            int u, v;
            scanf("%d %d", &u, &v);

            // update matrix TODO
        }

        // analyze matrix TODO
    }
}
